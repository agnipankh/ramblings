Add the following code right after the post-meta span in loop.html:

```html
{% if SHOW_COVER_IN_LISTING|default(False) and article.cover %}
    <div class="post-cover-image">
        {% if article.cover|lower|truncate(4, True, '') == "http" %}
            {% set featured_image = article.cover %}
        {% elif article.cover.startswith('/') %}
            {% set featured_image = SITEURL+article.cover %}
        {% else %}
            {% set featured_image = SITEURL+"/"+article.cover %}
        {% endif %}
        <a href="{{ SITEURL }}/{{ article.url }}">
            <img src="{{ featured_image }}" alt="{{ article.title }}">
        </a>
    </div>
{% endif %}
```

This should be inserted between:

```html
<span class="post-meta">
    ...
</span>

<!-- INSERT THE CODE HERE -->

{% if SHOW_FULL_ARTICLE_IN_SUMMARY|default(False) %}
    ...
```

Save this file to your theme at:
/Users/amit/Dropbox/DCode/ramblings/themes/attila/templates/partials/loop.html
